{
  "name": "@toolboard/tb-api",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "tsc-watch --noClear --silent",
    "build": "rimraf lib/ && tsc",
    "generate": "npx swagger-typescript-api -p ./../../../docs/http-server/apidoc/swagger.json --add-readonly --route-types -o ./ -n httpApi.ts"
  },
  "dependencies": {
    "swagger-typescript-api": "^12.0.4"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsc-watch": "^6.0.0",
    "typescript": "^4.6.4"
  }
}
